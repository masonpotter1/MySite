# Public site URL (no trailing slash). Used for canonical, Open Graph, Twitter, JSON-LD, and sitemap.xml at build time.
# Set in Vercel Project → Settings → Environment Variables for Production (and Preview if you want OG URLs on previews).
# Example:
# VITE_SITE_URL=https://yourdomain.com
VITE_SITE_URL=
